package com.lease.web.app.repository;

import com.lease.model.entity.ProvinceInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProvinceInfoRepository extends JpaRepository<ProvinceInfo, Long> {
}
